interface Props {
  User: {
    id: string;
    objectId: string;
    username: string;
    socialAccount: string;
    avatar: string;
  };
  btnTitle : string;
}

const OnBoardingCard = ({ User, btnTitle }: Props) => {
  return (
    <div>
     
    </div>
  );
};

export default OnBoardingCard;
